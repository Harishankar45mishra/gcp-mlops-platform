apiVersion: v1
kind: Service

metadata:
  name: ml-api
  namespace: ml-api
  labels:
    app: ml-api
    release: monitoring

spec:
  selector:
    app: ml-api

  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP

  type: LoadBalancer
