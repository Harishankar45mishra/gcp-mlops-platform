apiVersion: v1
kind: Service

metadata:
  name: iris-api
  namespace: ml-api
  labels:
    app: iris-api
    release: monitoring

spec:
  selector:
    app: iris-api

  ports:
  - name: http
    port: 8000
    targetPort: 8000

  type: LoadBalancer
